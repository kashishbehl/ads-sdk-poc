{
  "name": "ads-widget",
  "version": "1.0.0",
  "description": "Injectable widget with iframe support",
  "scripts": {
    "build": "NODE_ENV=production webpack --mode production --config-name injectable",
    "build:dev": "NODE_ENV=development webpack --mode development --config-name injectable",
    "dev": "NODE_ENV=development webpack serve --mode development --config-name injectable",
    "watch": "NODE_ENV=development webpack --mode development --config-name injectable --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["widget", "injectable", "iframe"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.22.0",
    "@babel/preset-env": "^7.22.0",
    "babel-loader": "^9.1.2",
    "css-loader": "^6.8.1",
    "html-webpack-plugin": "^5.5.1",
    "style-loader": "^3.3.3",
    "svelte": "^4.0.0",
    "svelte-loader": "^3.1.9",
    "webpack": "^5.85.0",
    "webpack-cli": "^5.1.1",
    "webpack-dev-server": "^4.15.0"
  },
  "peerDependencies": {
    "svelte": "^4.0.0"
  }
}
